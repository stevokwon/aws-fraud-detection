{
  "name": "aws-fraud-detection",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".devcontainer"
  },
  "postCreateCommand": "airflow db init && airflow users create -u admin -p admin -f admin -l user -r Admin -e admin@example.com",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": ["ms-python.python"]
    }
  },
  "forwardPorts": [8080],
  "remoteUser": "root"
}
